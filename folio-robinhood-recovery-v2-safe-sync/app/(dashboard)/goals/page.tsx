import {Card} from "@/components/ui/card";import {money} from "@/lib/utils";const goals=[{name:'House down payment',current:64200,target:100000,date:'Dec 2027'},{name:'Financial independence',current:112450,target:1000000,date:'Jan 2042'},{name:'Emergency fund',current:14820,target:20000,date:'Oct 2026'}];export default function Page(){return <div><h1 className="text-3xl font-semibold">Goals</h1><p className="mt-1 text-sm text-zinc-500">Connect daily investing to long-term outcomes.</p><div className="mt-6 grid gap-4 lg:grid-cols-2">{goals.map(g=>{const p=Math.min(100,g.current/g.target*100);return <Card key={g.name} className="p-5"><div className="flex justify-between"><div><h2 className="font-medium">{g.name}</h2><p className="text-xs text-zinc-500">Target {g.date}</p></div><span className="text-sm text-emerald-400">{p.toFixed(1)}%</span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-white/[.06]"><div className="h-full rounded-full bg-emerald-400" style={{width:`${p}%`}}/></div><div className="mt-3 flex justify-between text-sm"><span>{money(g.current)}</span><span className="text-zinc-500">{money(g.target)}</span></div></Card>})}</div></div>}
