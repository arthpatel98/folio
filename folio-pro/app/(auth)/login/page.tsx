"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {Button} from "@/components/ui/button";import {Input} from "@/components/ui/input";import {TrendingUp} from "lucide-react";import {toast} from "sonner";export default function Page(){const [email,setEmail]=useState('');const signIn=async()=>{if(!process.env.NEXT_PUBLIC_SUPABASE_URL)return toast.error('Add Supabase environment variables first.');const {error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});error?toast.error(error.message):toast.success('Check your email for the secure sign-in link.')};return <main className="grid min-h-screen place-items-center p-4"><div className="glass w-full max-w-md rounded-3xl p-7"><div className="mb-8 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-emerald-400 text-zinc-950"><TrendingUp/></div><div><h1 className="text-xl font-semibold">Welcome to Arth’s Portfolios</h1><p className="text-sm text-zinc-500">Your investments, understood.</p></div></div><label className="text-sm text-zinc-400">Email address</label><Input className="mt-2" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" type="email"/><Button className="mt-4 w-full" onClick={signIn}>Continue with email</Button><p className="mt-5 text-center text-xs leading-5 text-zinc-600">Passwordless authentication powered by Supabase. Demo pages remain accessible without configuration.</p></div></main>}
