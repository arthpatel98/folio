"use client"; import {Cell,Pie,PieChart,ResponsiveContainer,Tooltip} from "recharts"; import {holdings} from "@/lib/data/mock"; import {holdingMetrics} from "@/lib/calculations/portfolio"; import {money} from "@/lib/utils";
const colors=['#34d399','#22d3ee','#a78bfa','#f59e0b','#fb7185','#60a5fa','#f472b6','#94a3b8'];
export function AllocationChart(){const map=new Map<string,number>();holdings.forEach(h=>map.set(h.sector,(map.get(h.sector)||0)+holdingMetrics(h).marketValue));const data=[...map].map(([name,value])=>({name,value}));return <div className="grid items-center gap-4 sm:grid-cols-2"><div className="h-56"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={65} outerRadius={92} paddingAngle={3}>{data.map((_,i)=><Cell key={i} fill={colors[i%colors.length]}/>)}</Pie><Tooltip formatter={(v:number)=>money(v)} contentStyle={{background:'#111113',border:'1px solid rgba(255,255,255,.1)',borderRadius:14}}/></PieChart></ResponsiveContainer></div><div className="space-y-2">{data.map((d,i)=><div key={d.name} className="flex items-center justify-between text-sm"><span className="flex items-center gap-2 text-zinc-400"><span className="size-2 rounded-full" style={{background:colors[i%colors.length]}}/>{d.name}</span><span>{money(d.value)}</span></div>)}</div></div>}
