"use client"; import {motion} from "framer-motion"; import {ArrowDownRight,ArrowUpRight} from "lucide-react"; import {Card} from "@/components/ui/card"; import {cn} from "@/lib/utils";
export function MetricCard({label,value,change,subtle}:{label:string;value:string;change?:number;subtle?:string}){const up=(change??0)>=0;return <motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}}><Card className="p-5"><div className="text-sm text-zinc-500">{label}</div><div className="mt-2 text-2xl font-semibold tracking-tight">{value}</div>{change!==undefined?<div className={cn("mt-2 flex items-center gap-1 text-xs",up?"positive":"negative")}>{up?<ArrowUpRight size={14}/>:<ArrowDownRight size={14}/>} {Math.abs(change).toFixed(2)}%</div>:<div className="mt-2 text-xs text-zinc-600">{subtle}</div>}</Card></motion.div>}
